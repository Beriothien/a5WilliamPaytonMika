Drop Table USERS;

Create Table USERS (
    ID INT NOT NULL AUTO_INCREMENT,
    LAST_NAME VARCHAR(50), 
    FIRST_NAME VARCHAR(50),
    PASSWORD VARCHAR(50),
    PHONE VARCHAR(12),
    EMAIL VARCHAR(50),
    HOMETOWN CHAR(20),
    GENDER CHAR(10),
    LANGUAGES CHAR(80),
    PRIMARY KEY (ID)
);

Insert into CUSTOMER (LAST_NAME, FIRST_NAME,PASSWORD,PHONE, EMAIL, HOMETOWN,GENDER,LANGUAGES) VALUES('', '', '', '','','','','');
